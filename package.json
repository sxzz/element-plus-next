{
  "private": true,
  "packageManager": "pnpm@6.32.11",
  "description": "Element Plus Monorepo.",
  "license": "MIT",
  "engines": {
    "node": ">= 16"
  },
  "scripts": {
    "dev": "pnpm -C playground dev",
    "dev:node": "pnpm --filter ./packages/node run dev --parallel",
    "build": "pnpm run build:node && pnpm run build:web",
    "build:node": "pnpm --filter ./packages/node run build --parallel",
    "build:web": "pnpm --filter ./packages/web run build --parallel",
    "test": "vitest",
    "typecheck": "pnpm vue-tsc --noEmit -p tsconfig.web.json --composite false && pnpm vue-tsc --noEmit -p tsconfig.playground.json --composite false && pnpm tsc --noEmit -p tsconfig.node.json && pnpm tsc --noEmit -p tsconfig.vite-config.json && pnpm vue-tsc --noEmit -p tsconfig.vitest.json --composite false"
  },
  "devDependencies": {
    "@element-plus-dev/eslint-config": "workspace:*",
    "@types/jsdom": "^16.2.14",
    "@types/node": "*",
    "@vitest/ui": "^0.10.0",
    "c8": "^7.11.2",
    "jiti": "^1.13.0",
    "jsdom": "^19.0.0",
    "vitest": "^0.10.0"
  }
}
