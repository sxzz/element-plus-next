{
  "private": true,
  "packageManager": "pnpm@6.32.3",
  "description": "Element Plus Monorepo.",
  "license": "MIT",
  "engines": {
    "node": ">= 16"
  },
  "scripts": {
    "dev": "pnpm -C playground dev",
    "dev:cli": "pnpm --filter @element-plus-dev/build* run dev --parallel",
    "build": "pnpm run build:cli && pnpm run build:app",
    "build:cli": "pnpm --filter @element-plus-dev/build* run build --parallel",
    "build:app": "pnpm --filter @element-plus-dev/theme-chalk --filter @element-plus-dev/vue* --filter @element-plus-dev/utils --filter @element-plus-dev/constants run build --parallel",
    "test": "vitest",
    "typecheck": "pnpm vue-tsc --noEmit -p tsconfig.web.json --composite false && pnpm vue-tsc --noEmit -p tsconfig.playground.json --composite false && pnpm tsc --noEmit -p tsconfig.node.json && pnpm tsc --noEmit -p tsconfig.vite-config.json && pnpm vue-tsc --noEmit -p tsconfig.vitest.json --composite false"
  },
  "devDependencies": {
    "@element-plus-dev/eslint-config": "workspace:*",
    "@types/jsdom": "^16.2.14",
    "@types/node": "*",
    "c8": "^7.11.0",
    "jiti": "^1.13.0",
    "jsdom": "^19.0.0",
    "vitest": "^0.6.1"
  }
}
